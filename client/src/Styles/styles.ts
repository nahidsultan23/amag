const container = {
    width: "800px",
    margin: "0 auto",
    padding: "10px 10px",
    borderRadius: "5px",
    fontFamily: "Roboto, sans-serif",
    fontWeight: "700",
    color: "#5b6466",
    border: "1px solid #c4c4c4",
};

const buttonsContainer = {
    height: "50px",
};

const buttonSave = { fontWeight: "bold" };

const buttonSaveContainer = {
    display: "inline-block",
};

const buttonCancel = {
    color: "#212121",
    borderColor: "#c4c4c4",
    fontWeight: "bold",
};

const buttonCancelContainer = {
    display: "inline-block",
    marginLeft: "10px",
};

const formContainer = {
    marginTop: "15px",
};

const siteIdContainer = {
    height: "50px",
    paddingLeft: "15px",
};

const textFieldContainer = {
    height: "75px",
};

const textFieldFullWidth = { width: "100%" };

const textFieldInlineLeft = {
    display: "inline-block",
};

const textFieldInlineRight = {
    display: "inline-block",
    marginLeft: "50px",
};

const audiLogContainer = {
    backgroundColor: "#f2f2f2",
    padding: "20px 20px",
};

const audiLogTitle = {
    color: "#939393",
    fontSize: "18px",
    height: "30px",
};

const recordContainer = {
    marginTop: "15px",
};

const singleRecordContainer = {
    height: "20px",
    fontSize: "14px",
};

export const styles = {
    container,
    buttonsContainer,
    buttonSave,
    buttonSaveContainer,
    buttonCancel,
    buttonCancelContainer,
    formContainer,
    siteIdContainer,
    textFieldContainer,
    textFieldFullWidth,
    textFieldInlineLeft,
    textFieldInlineRight,
    audiLogContainer,
    audiLogTitle,
    recordContainer,
    singleRecordContainer,
};
